import 'package:flutter/material.dart';

// Light purple background (from your image)
const Color kBackgroundColor = Color(0xFFF3F0FA);

// Pinkish-tan accent (from your icon)
const Color kAccentColor = Color(0xFFD9B6B0);

// Optional: Text color for contrast
const Color kTextColor = Color(0xFF444444);

// Optional: Use for buttons or highlights
const Color kPrimaryColor = kAccentColor;